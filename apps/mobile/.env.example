# Base URL of the GreatSales API. Use your machine's LAN IP (not localhost) when
# running the app on a physical device so it can reach your dev server.
EXPO_PUBLIC_API_URL=http://localhost:3000/api/v1
