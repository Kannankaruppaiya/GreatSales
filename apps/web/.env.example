# GreatSales web admin — environment
# Base URL of the GreatSales API (include the /api/v1 prefix, no trailing slash).
# VITE_* vars are inlined into the client bundle at build time.
VITE_API_URL=http://localhost:3000/api/v1
# Optional: shown in the Account footer.
VITE_APP_VERSION=1.0.0
